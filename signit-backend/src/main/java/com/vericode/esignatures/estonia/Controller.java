package com.vericode.esignatures.estonia;

public class Controller {
    public boolean validateContainer(String container) {
        // Dummy implementation for illustration purposes
        return container != null && !container.isEmpty();
    }
}
